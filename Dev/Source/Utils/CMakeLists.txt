CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(TARGET_NAME FreUtils)

FILE(GLOB_RECURSE FILES Public/*.* Private/*.*)

ADD_LIBRARY(${TARGET_NAME} ${FILES})

TARGET_INCLUDE_DIRECTORIES(${TARGET_NAME}
	PUBLIC 
		Public
		${CMAKE_CURRENT_BINARY_DIR}
)