CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(PROJECT_NAME ${ENGINE_NAME})

FILE(GLOB_RECURSE HEADER_FILES Include/*.h Include/*.inc)
FILE(GLOB_RECURSE SOURCE_FILES Source/*.*)

SOURCE_GROUP(RDInterface REGULAR_EXPRESSION "RD *")

INCLUDE_DIRECTORIES(
	${FRE_DEV_COMMON_DIR}
	${MATH_DIRS}
	${CMAKE_CURRENT_SOURCE_DIR}/Include
	${CMAKE_CURRENT_SOURCE_DIR}/Include/Math
	${CMAKE_CURRENT_SOURCE_DIR}/Include/Containers
	${CMAKE_CURRENT_SOURCE_DIR}/Include/Misc
	#${CMAKE_CURRENT_SOURCE_DIR}/../Output/Include
	)
  
ADD_DEFINITIONS(-D_FRE4_EXPORTS)
ADD_DEFINITIONS(-D_RD_EXPORT)
ADD_DEFINITIONS(-D_USE_MATH_DEFINES)

ADD_LIBRARY(${PROJECT_NAME} SHARED ${HEADER_FILES} ${SOURCE_FILES})
#TARGET_LINK_LIBRARIES(${PROJECT_NAME} Output)

INSTALL(TARGETS ${PROJECT_NAME} DESTINATION "C:\\Temp\\FRE_INSTALL")

FILE(GLOB COMMON_FILES ${FRE_DEV_COMMON_DIR}/*.*)
INSTALL(FILES ${HEADER_FILES} ${COMMON_FILES} DESTINATION "C:\\Temp\\FRE_INSTALL\\Include")

INCLUDE(RDExportMethods.cmake)



