cmake_minimum_required(VERSION 2.6)

file(GLOB_RECURSE HEADER_FILES Include/*.h)
file(GLOB_RECURSE SOURCE_FILES Source/*.*)

list(REMOVE_AT HEADER_FILES config.h)
configure_file(${CMAKE_CURRENT_SOURCE_DIR}/config.h.in ${CMAKE_CURRENT_SOURCE_DIR}/Include/config.h)

include_directories(
	${FRE_DEV_COMMON_DIR}
	${FRE_MATH_DIRS}
	${CMAKE_CURRENT_SOURCE_DIR}/Include
	)
  
add_definitions(-D_FRE4_EXPORTS)
add_definitions(-D_USE_MATH_DEFINES)

add_library(Engine SHARED ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADERS} ${PLATFORM_SOURCES})

#set(INSTALL_HEADER_FILES ${HEADER_FILES})
#file(GLOB INSTALL_HEADER_FILES ${FRE_MATH_DIRS}/*.h)
#file(GLOB INSTALL_HEADER_FILES ${FRE_DEV_COMMON_DIR}/*.h)
#install(FILES ${INSTALL_HEADER_FILES} DESTINATION ${FRE_ENGINE_INSTALL_INCLUDE_DIRS})

#export(TARGETS Engine FILE Engine-exports.cmake)

install(DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Include" ${FRE_DEV_COMMON_DIR} ${FRE_MATH_DIRS} DESTINATION ${FRE_ENGINE_INSTALL_INCLUDE_DIRS} FILES_MATCHING PATTERN "*.h")

#set(FRE_ENGINE_INCLUDE_DIRS "${CMAKE_CURRENT_SOURCE_DIR}/INCLUDE" "${FRE_DEV_COMMON_DIR}" "${FRE_MATH_DIRS}")
#message(${FRE_ENGINE_INCLUDE_DIRS}) 